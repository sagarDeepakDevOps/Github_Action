name: variables
on: push
jobs:
  generate-a-secret-output:
    runs-on: ubuntu-latest
    steps:
      - id: deafult_var
        name: cheking deafualt variable name
      - uses: actions/checkout@v4
      
        run: echo "$GITHUB_ACTION"