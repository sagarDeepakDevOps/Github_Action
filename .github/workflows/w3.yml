name: variables
on: push
jobs:
  generate-a-secret-output:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - id: default_var
        name: checking default variable name
        run: echo "$GITHUB_ACTION"